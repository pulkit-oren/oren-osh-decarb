import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "Decarbonization Planner · Oren OSH",
  description:
    "Model Scope 1 and Scope 2 decarbonization scenarios on real operational data — electrification, fuel switching, refrigerants, efficiency, solar and renewable procurement against an SBTi 1.5°C target.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full">{children}</body>
    </html>
  );
}
